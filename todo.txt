Run des tests

no error message when connecting without pass
error sent after disconection when using wrong pass
op only cmd like kick should send notop instead of notenough param if not op
when joining invite only channel without being invited receive Not enough parameters instead of ERR_INVITEONLYCHAN
when joining key channel without key return not enough arg error instead of ERR_BADCHANNELKEY
when joining a key channel with wrong key segfault
using mode cmd with invalid arguments like arguments without the + or - does't return notop message